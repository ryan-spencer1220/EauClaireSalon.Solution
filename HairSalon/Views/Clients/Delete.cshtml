@{
  Layout = "_Layout";
}

@model HairSalon.Models.Client

<h2 class="display-4 lh-1 mb-4">Are you sure you want to remove @Html.DisplayFor(model => model.FirstName)?</h2>

@using (Html.BeginForm())
{
    <div class="row justify-content-center">
      <div class="col-sm-4">
        <input type="submit" value="Delete" class="btn btn-primary"/>
      </div>
    </div>
}

<div class="row justify-content-center">
  <div class="col-sm-4">
    @Html.ActionLink("Back to List", "Index", routeValues: null, htmlAttributes: new { @class = "btn btn-primary", @role="button" })
  </div>
</div>